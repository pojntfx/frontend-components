import styled from "@emotion/styled";
import MasonryTemplate from "react-masonry-component";

const GalleryWrapper = styled(MasonryTemplate)`
  margin-top: -1em;
  margin-bottom: -1em;
`;

export { GalleryWrapper };
