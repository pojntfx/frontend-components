import styled from "@emotion/styled";
import {Card} from "semantic-ui-react";

const CoverflowItemTemplate = styled(Card)`
  margin-top: 1em !important;
  margin-bottom: 1em !important;
`;

export {CoverflowItemTemplate};
